const me = {
    name: "Bazuaye",
    age: 20,
};
const mr = {
    id: "",
};
document.addEventListener("DOMContentLoaded", () => {
    console.log(`Dom content has loaded.
	Javascript is running
	`);
    const myAry = [1, 2, 3];
    console.log(myAry);
    // Hello
});
export {};
